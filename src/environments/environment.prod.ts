export const environment = {
  production: true,
  apiUrl: 'https://api.quotable.io', // For quotes API
  authApiUrl: '/auth', // Will use mock auth in production
  useMockAuth: true, // Use in-memory auth for GitHub Pages
};
